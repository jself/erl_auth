{disc_nodes, [auth@localhost]}.
{profiles_table, [
    {disc_copies, [auth@localhost]}
]}.
{sessions_table, [
    {ram_copies, [auth@localhost]}
]}.
{profiledata_table, [
    {disc_only_copies, [auth@localhost]}
]}.
{session_length, 0}.
